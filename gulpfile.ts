import { loadSeedTasks } from './gulpfile.seed';

loadSeedTasks();

/**
 *  It is preferable to configure tasks in the `./tools/config/project.tasks.json`.
 *
 *  If you need to temporarily test tasks quickly, you may place them here, then
 *  migrate them to `./tools/config/project.tasks.json`.
 */
